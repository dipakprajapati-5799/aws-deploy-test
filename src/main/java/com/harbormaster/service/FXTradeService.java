/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>FXTrade business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of FXTrade related services in the case of a FXTrade business related service failing.</li>
 * <li>Exposes a simpler, uniform FXTrade interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill FXTrade business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/08/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class FXTradeService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public FXTradeService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new FXTradeEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(FXTradeRepository.class) );
		this.validator		= applicationContext.getBean(FXTradeValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		FXTrade
	 */
		public FXTrade createFXTrade( CreateFXTradeCommand command ) {

		FXTrade entity = new FXTrade();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setFXTradeId( command.getFXTradeId() );
            entity.setTradeReference( command.getTradeReference() );
            entity.setTradeDate( command.getTradeDate() );
            entity.setSettlementDate( command.getSettlementDate() );
            entity.setAmountSold( command.getAmountSold() );
            entity.setAmountBought( command.getAmountBought() );
            entity.setRate( command.getRate() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of FXTrade {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateFXTradeCommand
	 * @return		FXTrade
	 */
	public FXTrade updateFXTrade( UpdateFXTradeCommand command ) {

		FXTrade entity = new FXTrade();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setFXTradeId( command.getFXTradeId() );
            entity.setTradeReference( command.getTradeReference() );
            entity.setTradeDate( command.getTradeDate() );
            entity.setSettlementDate( command.getSettlementDate() );
            entity.setAmountSold( command.getAmountSold() );
            entity.setAmountBought( command.getAmountBought() );
            entity.setRate( command.getRate() );
            entity.setCustomer( command.getCustomer() );
            entity.setBank( command.getBank() );
            entity.setExchangeRate( command.getExchangeRate() );
            entity.setSourceAccount( command.getSourceAccount() );
            entity.setDestinationAccount( command.getDestinationAccount() );
            entity.setTransaction( command.getTransaction() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of FXTrade {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteFXTradeCommand
	 */
	public void delete( DeleteFXTradeCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getFXTradeId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of FXTrade {0} ", id );

	}

	/**
	 * Method to retrieve the FXTrade via FXTradeFetchOneSummary
	 * @param 	summary FXTradeFetchOneSummary
	 * @return 	FXTradeFetchOneResponse
	 */
	public FXTrade getFXTrade( FXTradeFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "FXTradeFetchOneSummary arg cannot be null" );

		FXTrade entity = null;
		UUID id = summary.getFXTradeId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a FXTrade using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all FXTrades
	 *
	 * @return 	List<FXTrade>
	 */
    public List<FXTrade> getAllFXTrade() {
		List<FXTrade> list = projector.findAll( new FindAllFXTradeQuery() );
		return list;
	}

	/**
	 * assign Customer on FXTrade
	 * @param		command AssignCustomerToFXTradeCommand
	 */
	public void assignCustomer( AssignCustomerToFXTradeCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignCustomer(command.getFXTradeId(), command.getAssignment());

	}

	/**
	 * unAssign Customer on FXTrade
	 * @param		command UnAssignCustomerFromFXTradeCommand
	 */
	public void unAssignCustomer( UnAssignCustomerFromFXTradeCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignCustomer(command.getFXTradeId());
	}
	
	/**
	 * assign Bank on FXTrade
	 * @param		command AssignBankToFXTradeCommand
	 */
	public void assignBank( AssignBankToFXTradeCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignBank(command.getFXTradeId(), command.getAssignment());

	}

	/**
	 * unAssign Bank on FXTrade
	 * @param		command UnAssignBankFromFXTradeCommand
	 */
	public void unAssignBank( UnAssignBankFromFXTradeCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignBank(command.getFXTradeId());
	}
	
	/**
	 * assign ExchangeRate on FXTrade
	 * @param		command AssignExchangeRateToFXTradeCommand
	 */
	public void assignExchangeRate( AssignExchangeRateToFXTradeCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignExchangeRate(command.getFXTradeId(), command.getAssignment());

	}

	/**
	 * unAssign ExchangeRate on FXTrade
	 * @param		command UnAssignExchangeRateFromFXTradeCommand
	 */
	public void unAssignExchangeRate( UnAssignExchangeRateFromFXTradeCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignExchangeRate(command.getFXTradeId());
	}
	
	/**
	 * assign SourceAccount on FXTrade
	 * @param		command AssignSourceAccountToFXTradeCommand
	 */
	public void assignSourceAccount( AssignSourceAccountToFXTradeCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignSourceAccount(command.getFXTradeId(), command.getAssignment());

	}

	/**
	 * unAssign SourceAccount on FXTrade
	 * @param		command UnAssignSourceAccountFromFXTradeCommand
	 */
	public void unAssignSourceAccount( UnAssignSourceAccountFromFXTradeCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignSourceAccount(command.getFXTradeId());
	}
	
	/**
	 * assign DestinationAccount on FXTrade
	 * @param		command AssignDestinationAccountToFXTradeCommand
	 */
	public void assignDestinationAccount( AssignDestinationAccountToFXTradeCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignDestinationAccount(command.getFXTradeId(), command.getAssignment());

	}

	/**
	 * unAssign DestinationAccount on FXTrade
	 * @param		command UnAssignDestinationAccountFromFXTradeCommand
	 */
	public void unAssignDestinationAccount( UnAssignDestinationAccountFromFXTradeCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignDestinationAccount(command.getFXTradeId());
	}
	
	/**
	 * assign Transaction on FXTrade
	 * @param		command AssignTransactionToFXTradeCommand
	 */
	public void assignTransaction( AssignTransactionToFXTradeCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignTransaction(command.getFXTradeId(), command.getAssignment());

	}

	/**
	 * unAssign Transaction on FXTrade
	 * @param		command UnAssignTransactionFromFXTradeCommand
	 */
	public void unAssignTransaction( UnAssignTransactionFromFXTradeCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignTransaction(command.getFXTradeId());
	}
	




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final FXTradeEntityProjector projector;
	private final FXTradeValidator validator;
	private FXTrade fXTrade 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(FXTradeService.class);
}
