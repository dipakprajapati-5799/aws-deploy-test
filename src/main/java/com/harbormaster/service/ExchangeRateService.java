/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>ExchangeRate business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of ExchangeRate related services in the case of a ExchangeRate business related service failing.</li>
 * <li>Exposes a simpler, uniform ExchangeRate interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill ExchangeRate business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/08/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class ExchangeRateService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public ExchangeRateService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new ExchangeRateEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(ExchangeRateRepository.class) );
		this.validator		= applicationContext.getBean(ExchangeRateValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		ExchangeRate
	 */
		public ExchangeRate createExchangeRate( CreateExchangeRateCommand command ) {

		ExchangeRate entity = new ExchangeRate();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setExchangeRateId( command.getExchangeRateId() );
            entity.setBaseCurrency( command.getBaseCurrency() );
            entity.setCounterCurrency( command.getCounterCurrency() );
            entity.setRate( command.getRate() );
            entity.setAsOf( command.getAsOf() );
            entity.setSource( command.getSource() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of ExchangeRate {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateExchangeRateCommand
	 * @return		ExchangeRate
	 */
	public ExchangeRate updateExchangeRate( UpdateExchangeRateCommand command ) {

		ExchangeRate entity = new ExchangeRate();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setExchangeRateId( command.getExchangeRateId() );
            entity.setBaseCurrency( command.getBaseCurrency() );
            entity.setCounterCurrency( command.getCounterCurrency() );
            entity.setRate( command.getRate() );
            entity.setAsOf( command.getAsOf() );
            entity.setSource( command.getSource() );
            entity.setBank( command.getBank() );
            entity.setFxTrades( command.getFxTrades() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of ExchangeRate {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteExchangeRateCommand
	 */
	public void delete( DeleteExchangeRateCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getExchangeRateId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of ExchangeRate {0} ", id );

	}

	/**
	 * Method to retrieve the ExchangeRate via ExchangeRateFetchOneSummary
	 * @param 	summary ExchangeRateFetchOneSummary
	 * @return 	ExchangeRateFetchOneResponse
	 */
	public ExchangeRate getExchangeRate( ExchangeRateFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "ExchangeRateFetchOneSummary arg cannot be null" );

		ExchangeRate entity = null;
		UUID id = summary.getExchangeRateId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a ExchangeRate using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all ExchangeRates
	 *
	 * @return 	List<ExchangeRate>
	 */
    public List<ExchangeRate> getAllExchangeRate() {
		List<ExchangeRate> list = projector.findAll( new FindAllExchangeRateQuery() );
		return list;
	}

	/**
	 * assign Bank on ExchangeRate
	 * @param		command AssignBankToExchangeRateCommand
	 */
	public void assignBank( AssignBankToExchangeRateCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignBank(command.getExchangeRateId(), command.getAssignment());

	}

	/**
	 * unAssign Bank on ExchangeRate
	 * @param		command UnAssignBankFromExchangeRateCommand
	 */
	public void unAssignBank( UnAssignBankFromExchangeRateCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignBank(command.getExchangeRateId());
	}
	

	/**
	 * add FXTrade to FxTrades
	 * @param		command AssignFxTradesToExchangeRateCommand
	 */
	public void addToFxTrades( AssignFxTradesToExchangeRateCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToFxTrades(command.getExchangeRateId(), command.getAddTo());
	}

	/**
	 * remove FXTrade from FxTrades
	 * @param		command RemoveFxTradesFromExchangeRateCommand
	 */
	public void removeFromFxTrades( RemoveFxTradesFromExchangeRateCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromFxTrades(command.getExchangeRateId(), command.getRemoveFrom());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final ExchangeRateEntityProjector projector;
	private final ExchangeRateValidator validator;
	private ExchangeRate exchangeRate 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(ExchangeRateService.class);
}
